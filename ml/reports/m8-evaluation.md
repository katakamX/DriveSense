# M8 — model training and evaluation

Generated by `python -m pipelines.train`. Every number here comes from that command; none was typed in by hand. The Parquet inputs are gitignored (`data/**`), so this report plus `ml/configs/fold_manifest_v1.json` is what a reviewer without the data can check.

- config: `ml/configs/train_v1.yaml` (version 1)
- feature version: `1`
- git sha: `66a0e7b6737e8492c47f16e2b65d9e5d1922774d`
- generated at: `2026-08-10T15:57:01.072272+00:00`
- label source: `rubric_label` — weak supervision (ADR 0006), not ground truth

## 0. Bottom line

On held-out simulator variants both models comfortably beat the majority-class baseline: logistic regression reaches macro-F1 0.843 ± 0.079, the decision tree 0.944 ± 0.019, against a baseline of 0.113 ± 0.015.

**On real UAH telemetry, neither beats the majority-class baseline.** The logistic regression scores 0.302 accuracy against a baseline of 0.650; the decision tree scores 0.539 against the same 0.650. This is the central result of M8 and it is stated first rather than at the end of section 7, because a reader who takes only one number away should take this one.

Two things follow, and they matter more than the headline simulator scores:

1. **The in-domain winner is the out-of-domain loser.** The decision tree wins on every simulator fold (macro-F1 0.944 vs 0.843) and has perfect `HIGH_RISK` recall there — then predicts `HIGH_RISK` **zero times** across all 1709 UAH windows. Its cross-validated score was not wrong; it simply measured generalisation to unseen *scripts*, which turns out to say little about generalisation to unseen *driving*.
2. **The logistic regression's UAH failure is over-prediction, not silence.** It recovers all 16 `HIGH_RISK` windows (recall 1.000) but predicts that class 321 times, for a precision of 0.050. A model that calls one window in five high-risk is not usable as-is, whatever its recall says.

Section 7 sets out the three reasons this comparison is harder than "unseen data" — chiefly the measured simulator/real domain gap (M7b section 1). None of them make the result better; they explain it. The honest reading is that a model trained only on scripted simulator drives does not yet transfer to real telemetry, and `ml/README.md`'s standing instruction applies: that is a finding worth reporting, not hiding.

## 1. What was trained on

827 simulator windows from 74 recordings across 13 authored script variants. `sim-demo` is excluded outright: it is a pre-existing M2 demo artefact with no profile and no scripted intent, so a grouped split has nowhere to put it (M7b TODO 4).

| class | windows | share |
| --- | --- | --- |
| AGGRESSIVE | 177 | 21.4% |
| CALM | 247 | 29.9% |
| HIGH_RISK | 100 | 12.1% |
| NORMAL | 303 | 36.6% |
| **total** | **827** | |

**Features: 25 of 26.** The excluded ones, and why:

- **`rapid_accel_per_min`** — Identically 0.0 at every percentile and every maximum across all 1,709 UAH validation windows, while reaching 36.1/min on the simulator (ml/reports/m7b-simulator-generation-pilot-and-bulk.md, finding (a)). The 3.0 m/s^2 RAPID_ACCELERATION threshold is simply never crossed by real phone-accelerometer telemetry in this corpus, so the feature carries simulator-only signal: a model leaning on it would score well on simulator folds and lose exactly that much on UAH, for a reason that has nothing to do with driving. Excluded deliberately rather than silently — see docs/model-card.md.

No UAH row appears in any fold, in the final fit, or in any number in sections 2-6 (ADR 0006). Section 7 evaluates against UAH separately, and states what that is and is not evidence of.

## 2. The split

Leave-one-variant-out grouped 3-fold cross-validation. The group is the **script variant** (`aggressive-b`), not the recording: the six recordings of one variant differ only in `sensor_noise_seed`, so splitting by recording would put two near-copies of one authored drive on opposite sides and call the result generalisation. Every variant is held out exactly once, so the folds partition the corpus and each window has exactly one out-of-fold prediction.

| fold | held-out variants | recordings | windows | class balance of the held-out fold |
| --- | --- | --- | --- | --- |
| 0 | aggressive-b, calm-a, high_risk-a, high_risk-b, normal-b | 28 | 299 | AGGRESSIVE 65, CALM 90, HIGH_RISK 55, NORMAL 89 |
| 1 | aggressive-a, calm-b, high_risk-d, normal-a | 23 | 270 | AGGRESSIVE 53, CALM 97, HIGH_RISK 25, NORMAL 95 |
| 2 | aggressive-c, calm-c, high_risk-c, normal-c | 23 | 258 | AGGRESSIVE 59, CALM 60, HIGH_RISK 20, NORMAL 119 |

Fold assignment is pinned in `ml/configs/fold_manifest_v1.json` (seed `20260810`) and verified against the corpus on every run — if the corpus changes, training fails rather than quietly reporting numbers from a different split.

## 3. Headline results (mean ± sd across folds)

| model | accuracy | majority baseline | macro-F1 | balanced acc. | HIGH_RISK recall |
| --- | --- | --- | --- | --- | --- |
| majority-class baseline | 0.294 ± 0.049 | 0.294 ± 0.049 | **0.113 ± 0.015** | 0.250 ± 0.000 | 0.000 ± 0.000 |
| logistic regression | 0.824 ± 0.058 | 0.294 ± 0.049 | **0.843 ± 0.079** | 0.851 ± 0.083 | 0.939 ± 0.086 |
| decision tree | 0.944 ± 0.024 | 0.294 ± 0.049 | **0.944 ± 0.019** | 0.956 ± 0.013 | 1.000 ± 0.000 |

**Macro-F1 is the headline, not accuracy.** Accuracy is reported only next to the majority-class baseline it has to beat, because on a corpus where one class is 37% of windows — and on UAH, where one class is 65% — accuracy mostly measures the class balance. Macro-F1 weights all four classes equally, so a model that never predicts `HIGH_RISK` cannot hide behind the other three.

On macro-F1 the logistic regression (0.843 ± 0.079) **does not beat** the decision tree (0.944 ± 0.019). `ml/README.md` asks for this comparison to be reported either way, so it is reported: on held-out simulator variants, the tree is the better model by a clear margin.

**That margin does not survive contact with real data, and it is the reason the artefact choice is not merely a format decision.** `model.json`'s format is a coefficient dump (`pipelines.artifact`), which a tree cannot use — a tree would need its own node serialisation. That was the original justification; section 7 supplies a better one. The tree's advantage here is entirely in-domain: on UAH it never predicts HIGH_RISK at all, while the logistic regression at least keeps every class reachable. Shipping the model that degrades *loudly* rather than the one that degrades *silently* is the right call on a corpus this far from its target domain, independent of which scored higher above.

### majority-class baseline — per fold

| fold | windows | accuracy | majority baseline | macro-F1 | balanced acc. | HIGH_RISK recall |
| --- | --- | --- | --- | --- | --- | --- |
| 0 | 299 | 0.298 | 0.298 (NORMAL) | 0.115 | 0.250 | 0.000 |
| 1 | 270 | 0.352 | 0.352 (NORMAL) | 0.130 | 0.250 | 0.000 |
| 2 | 258 | 0.233 | 0.233 (CALM) | 0.094 | 0.250 | 0.000 |
| **mean ± sd** | — | **0.294 ± 0.049** | 0.294 ± 0.049 | **0.113 ± 0.015** | 0.250 ± 0.000 | 0.000 ± 0.000 |

### logistic regression — per fold

| fold | windows | accuracy | majority baseline | macro-F1 | balanced acc. | HIGH_RISK recall |
| --- | --- | --- | --- | --- | --- | --- |
| 0 | 299 | 0.759 | 0.298 (NORMAL) | 0.738 | 0.736 | 0.818 |
| 1 | 270 | 0.900 | 0.352 (NORMAL) | 0.930 | 0.930 | 1.000 |
| 2 | 258 | 0.814 | 0.233 (CALM) | 0.862 | 0.886 | 1.000 |
| **mean ± sd** | — | **0.824 ± 0.058** | 0.294 ± 0.049 | **0.843 ± 0.079** | 0.851 ± 0.083 | 0.939 ± 0.086 |

### decision tree — per fold

| fold | windows | accuracy | majority baseline | macro-F1 | balanced acc. | HIGH_RISK recall |
| --- | --- | --- | --- | --- | --- | --- |
| 0 | 299 | 0.967 | 0.298 (NORMAL) | 0.970 | 0.968 | 1.000 |
| 1 | 270 | 0.911 | 0.352 (NORMAL) | 0.927 | 0.938 | 1.000 |
| 2 | 258 | 0.953 | 0.233 (CALM) | 0.936 | 0.962 | 1.000 |
| **mean ± sd** | — | **0.944 ± 0.024** | 0.294 ± 0.049 | **0.944 ± 0.019** | 0.956 ± 0.013 | 1.000 ± 0.000 |

## 4. Per-class detail

### logistic regression — per class, mean ± sd across folds

| class | precision | recall | F1 | support (all folds) |
| --- | --- | --- | --- | --- |
| AGGRESSIVE | 0.896 ± 0.147 | 0.746 ± 0.291 | 0.800 ± 0.246 | 177 |
| CALM | 0.764 ± 0.125 | 0.945 ± 0.070 | 0.834 ± 0.069 | 247 |
| HIGH_RISK | 1.000 ± 0.000 | 0.939 ± 0.086 | 0.967 ± 0.047 | 100 |
| NORMAL | 0.797 ± 0.131 | 0.773 ± 0.094 | 0.773 ± 0.067 | 303 |

### decision tree — per class, mean ± sd across folds

| class | precision | recall | F1 | support (all folds) |
| --- | --- | --- | --- | --- |
| AGGRESSIVE | 0.943 ± 0.081 | 0.946 ± 0.042 | 0.940 ± 0.026 | 177 |
| CALM | 0.970 ± 0.043 | 0.933 ± 0.055 | 0.949 ± 0.015 | 247 |
| HIGH_RISK | 0.923 ± 0.109 | 1.000 ± 0.000 | 0.957 ± 0.061 | 100 |
| NORMAL | 0.922 ± 0.057 | 0.945 ± 0.047 | 0.932 ± 0.042 | 303 |

### Pooled out-of-fold confusion matrix — logistic regression

Every window scored by the one fold model that never saw its variant, pooled into a single matrix. Rows are the rubric label, columns the prediction.

| true \ predicted | AGGRESSIVE | CALM | HIGH_RISK | NORMAL | total |
| --- | --- | --- | --- | --- | --- |
| AGGRESSIVE | **128** | 0 | 0 | 49 | 177 |
| CALM | 0 | **231** | 0 | 16 | 247 |
| HIGH_RISK | 10 | 0 | **90** | 0 | 100 |
| NORMAL | 0 | 72 | 0 | **231** | 303 |

Row-normalised (per-class recall on the diagonal):

| true \ predicted | AGGRESSIVE | CALM | HIGH_RISK | NORMAL | total |
| --- | --- | --- | --- | --- | --- |
| AGGRESSIVE | **0.723** | 0.000 | 0.000 | 0.277 | 177 |
| CALM | 0.000 | **0.935** | 0.000 | 0.065 | 247 |
| HIGH_RISK | 0.100 | 0.000 | **0.900** | 0.000 | 100 |
| NORMAL | 0.000 | 0.238 | 0.000 | **0.762** | 303 |

| class | precision | recall | F1 | support | predicted |
| --- | --- | --- | --- | --- | --- |
| AGGRESSIVE | 0.928 | 0.723 | 0.813 | 177 | 138 |
| CALM | 0.762 | 0.935 | 0.840 | 247 | 303 |
| HIGH_RISK | 1.000 | 0.900 | 0.947 | 100 | 90 |
| NORMAL | 0.780 | 0.762 | 0.771 | 303 | 296 |

### HIGH_RISK recall, specifically

**0.939 ± 0.086 (min 0.818, max 1.000)** across folds, over 100 `HIGH_RISK` windows in total. This is the number the product's usefulness rests on: a model that scores well overall while missing high-risk driving is worse than useless, because it is confidently quiet about the only thing worth being loud about. It is called out separately so it cannot be averaged away.

For scale: those windows come from 4 script variants. Recall measured over that few authored drives has a wide confidence interval no matter what the point estimate says — the fold-to-fold spread above is a better guide to it than the mean.

### Degenerate-outcome guard

Flagged automatically: any class with zero recall, any class the model never predicts at all, and any model that fails to beat its own majority-class baseline.

No degenerate outcomes flagged: every class has non-zero recall, every class is predicted at least once, and every model beats its majority-class baseline.

## 5. Rubric as a classifier, against script intent

The rubric cannot be scored against `rubric_label`: it *is* `rubric_label`, so it would score 1.000 by construction and the number would mean nothing. The only non-circular check available is against **script intent** — the class each drive was authored to produce, recoverable from the recording id (`high_risk-c-seed1204` -> `HIGH_RISK`).

Two caveats, both material. First, intent is a *per-recording* label applied to every window inside it — the same granularity objection ADR 0006 raises against UAH's trip-level labels, and it applies here too: the idle windows at the start of an aggressive drive are not aggressive. Intent is not ground truth; it is what the script was aiming at. Second, M7b's standalone 45-window pilot recordings no longer exist as separate artefacts — bulk generation superseded them — so this runs over the full 827-window simulator corpus, which is the same comparison M7b section 3 reports, not the 45-window pilot table in its section 2.

| scored against script intent | accuracy | majority baseline | macro-F1 | balanced acc. |
| --- | --- | --- | --- | --- |
| rubric (the labeller itself) | 0.780 | 0.363 | **0.807** | 0.808 |
| logistic regression (out-of-fold) | 0.758 | 0.363 | **0.772** | 0.754 |
| decision tree (out-of-fold) | 0.788 | 0.363 | **0.805** | 0.808 |

The model is trained on rubric labels, so it can only recover intent to the extent the rubric does. Where it scores *lower* than the rubric, it has lost information the labeller had; it cannot score meaningfully higher, and if it appears to, that is a reason to look for a leak rather than to celebrate.

Rubric vs intent, as a confusion matrix (rows: intent, columns: rubric label):

| true \ predicted | AGGRESSIVE | CALM | HIGH_RISK | NORMAL | total |
| --- | --- | --- | --- | --- | --- |
| AGGRESSIVE | **144** | 0 | 0 | 0 | 144 |
| CALM | 13 | **174** | 0 | 71 | 258 |
| HIGH_RISK | 20 | 0 | **100** | 5 | 125 |
| NORMAL | 0 | 73 | 0 | **227** | 300 |

## 6. The shipped artefact

`ml/artifacts/model.json` is a **multinomial logistic regression** refitted on all 827 simulator windows with no fold held out, serialised as plain numbers by `pipelines.artifact` (format `drivesense-multinomial-logreg` v1) rather than pickled. The cross-validated numbers above estimate how this model generalises; they are not measurements of this exact fit, and this report does not present them as such.

The artefact carries 25 ordered feature names, a mean and scale per feature, and one coefficient row per class — everything needed to reproduce a prediction with arithmetic a reader can follow: `z = (x - standardiser.mean) / standardiser.scale; scores = coefficients @ z + intercepts; proba = softmax(scores); prediction = classes[argmax(proba)]`. Its round-trip against the fitted scikit-learn pipeline is asserted at training time and in `ml/tests/test_train.py`, so an artefact that disagrees with the model it came from fails the build rather than shipping.

`metadata.json` alongside it records the ordered feature names, the deliberate feature exclusion and its reason, the training date, dataset hash, git SHA, the fold manifest and the full metrics from this run.

## 7. UAH validation — real telemetry, never trained on

The final model predicting on all 1709 UAH-DriveSet windows. No UAH row was in any fold or in the final fit. Scored against `rubric_label`, since UAH's own labels are a different taxonomy at a different granularity (ADR 0006).

| model | accuracy | majority baseline | macro-F1 | balanced acc. | HIGH_RISK recall |
| --- | --- | --- | --- | --- | --- |
| logistic regression | 0.302 | 0.650 | **0.290** | 0.507 | 1.000 |
| decision tree | 0.539 | 0.650 | **0.385** | 0.458 | 0.000 |

| class | precision | recall | F1 | support | predicted |
| --- | --- | --- | --- | --- | --- |
| AGGRESSIVE | 0.167 | 0.413 | 0.238 | 366 | 902 |
| CALM | 0.587 | 0.375 | 0.458 | 216 | 138 |
| HIGH_RISK | 0.050 | 1.000 | 0.095 | 16 | 321 |
| NORMAL | 0.770 | 0.241 | 0.367 | 1111 | 348 |

Confusion matrix (rows: rubric label, columns: prediction):

| true \ predicted | AGGRESSIVE | CALM | HIGH_RISK | NORMAL | total |
| --- | --- | --- | --- | --- | --- |
| AGGRESSIVE | **151** | 1 | 210 | 4 | 366 |
| CALM | 50 | **81** | 9 | 76 | 216 |
| HIGH_RISK | 0 | 0 | **16** | 0 | 16 |
| NORMAL | 701 | 56 | 86 | **268** | 1111 |

Row-normalised:

| true \ predicted | AGGRESSIVE | CALM | HIGH_RISK | NORMAL | total |
| --- | --- | --- | --- | --- | --- |
| AGGRESSIVE | **0.413** | 0.003 | 0.574 | 0.011 | 366 |
| CALM | 0.231 | **0.375** | 0.042 | 0.352 | 216 |
| HIGH_RISK | 0.000 | 0.000 | **1.000** | 0.000 | 16 |
| NORMAL | 0.631 | 0.050 | 0.077 | **0.241** | 1111 |

### Degenerate-outcome guard, UAH

- **logistic regression, does not beat the majority-class baseline (0.302 vs 0.650)**
- **decision tree, DEGENERATE: zero recall on HIGH_RISK**
- **decision tree, does not beat the majority-class baseline (0.539 vs 0.650)**

### What this number is not

**It is not a clean held-out test, and this report does not claim it is.** Three things stand between this figure and that claim:

1. **The labels are rubric labels, and the rubric's cutoffs were calibrated on this corpus.** ADR 0006 is explicit: ten hand-set thresholds were chosen, once, by a human reading UAH's own feature percentiles. UAH's *labels* never trained or scored a model and no UAH row ever entered the training parquet — that guarantee holds — but the yardstick being measured against was shaped by the thing being measured.
2. **There is a domain gap, not just unseen data.** M7b section 1 measured it: the simulator is *smoother than real driving at the median and more extreme at the tails* — `accel_std` p50 of 0.127 against UAH's 0.229, but an absolute max of 2.529 against 1.311. This tests generalisation across two differently-shaped distributions, which is a harder and less interpretable question than generalisation to unseen samples of one.
3. **The class balance differs sharply.** `HIGH_RISK` is 12.1% of the training corpus and 0.9% of UAH (16 windows). Per-class figures on sixteen windows carry confidence intervals wide enough to swallow most conclusions.

### Cross-tab against UAH's own labels (qualitative only)

Not a metric — the taxonomies do not correspond and the granularities differ. It is a smell test: windows from trips UAH itself called `aggressive` should skew towards predicted `AGGRESSIVE`/`HIGH_RISK` more than windows from `normal` trips do. If they do not, something is wrong somewhere upstream.

| uah_label \ prediction | AGGRESSIVE | CALM | HIGH_RISK | NORMAL | total |
| --- | --- | --- | --- | --- | --- |
| aggressive | 175 | 7 | 156 | 8 | 346 |
| drowsy | 323 | 45 | 64 | 145 | 577 |
| normal | 404 | 86 | 101 | 195 | 786 |

Read as shares of each UAH label, which is the only way the skew is visible:

| UAH's own label | HIGH_RISK | AGGRESSIVE or HIGH_RISK | windows |
| --- | --- | --- | --- |
| aggressive | 45.1% | 95.7% | 346 |
| drowsy | 11.1% | 67.1% | 577 |
| normal | 12.8% | 64.2% | 786 |

The smell test half-passes, and the half that fails is the informative half. The **ordering is right**: trips UAH itself called `aggressive` draw a `HIGH_RISK` prediction several times more often than `normal` or `drowsy` trips do, and they top the combined risky share too. The model has not inverted the problem, and on a corpus it was never trained on that is worth something.

The **floor is what fails**: 64.2% of windows from trips UAH called `normal` are predicted `AGGRESSIVE` or `HIGH_RISK`. A severity ordering that only holds above a floor that high is not usable — it is the same over-prediction the per-class precision figures show from the other side, seen here against an independent set of labels rather than against the rubric's.

`drowsy` has no DriveSense equivalent and is expected to spread across the behaviour classes rather than concentrate — ADR 0006's stated limitation, that a vehicle-telemetry rubric cannot separate drowsiness from aggression as a *cause*, applies unchanged to a model trained on that rubric.
