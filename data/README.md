# data/

Datasets are **gitignored**. Simulator output is regenerated by
`simulator/`; public validation datasets are downloaded by a documented script
added in Milestone 7. Nothing here is a source of truth — everything is
reproducible from committed code and configuration.
